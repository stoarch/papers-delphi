Легенда:
x! - сколько дней назад было создано
!! - важно
!  - надо рассмотреть
» - рассматриваемый
√ - сделанный
? - оценка необходимости

*  - ошибка
+ - новшество

######################################################
Найденные баги и нововведения:

***( 24.12.04 )***
$$$ BUGS $$$
*[  ] Столбец "состояние" в списке документов пустой.

*[ ] При перерегистрации не желает заново заходить. Вызывает ошибку доступа.

$$$ NEW FEATURES $$$

+[  ] Необходимо проверять, что такой пользователь уже вошел. И сообщать при повторной
	попытке входа.

+[  ] Продумать систему архивации документов, а то через месяц их станет столько, что и 
	не управиться.

+[  ] Написать помощь

+[  ] Надо упорядочить модель и построить иерархически.

+[  ]  Поумнее продукт лоадер. Надо бы проверять, новее ли источник, и если нет, то
	просто запускать назначение.

+[  ] В TUserDataLoader загрузка данных сильно разделена по элементам, надо бы применить
	шаблон построитель, так будет удобно строить данные независимо от хранилища.


+[ ] Каждое рабочее место в отдельную DLL  (можно как ActiveX)

+[ ] Для каждого рабочего места своя версия и ProductLoader
	проверяет их и загружает только последние.

+[ ] Надо бы сделать систему автоматического обновления при запуске - указать на 
	загрузчик продукта, который бы проверял версию продукта и обновлял.

+[ ] Создать оповеститель о приходе новых документов

+[ ] Создать рабочее место офис менеджера

+[ ] Создать все рабочие места.


### IMPLEMENTED FEATURES ###
+[√] Надо сделать отображение департаментов в списке контактов и пользователей
	группировать по ним.

***( 23.12.04 )***
### IMPLEMENTED FEATURES ###
+[√] Сделать загрузку департаментов и сортировать пользователей по 	ним + реакцию на 		двойное нажатие в списке контактов.


***( 22.12.04 )***

### IMPLEMENTED FEATURES ###

+[√] Оптимизировать трафик, загружая все справочные таблицы в Environment

+[√] Загружать и кешировать список пользователей.

+[√] Продумать систему мягкой загрузки остальных документов, более умное добавление
	в список, чем пересоздание заново. Это нужно из-за медленной скорости.


### FIXED BUGS ###
*[√] При отсылке документов переходит на папку коммуникационных. А этого нельзя делать!

*[√] Не нумерует документы после их создания.

*[√] Очень долго обновляет данные, когда приходит новый документ

*[√] В коммуникационных документах надо сортировать по дате отсылке, а не 
	по дате созданию. Также во всех списках надо сортировать по отправителю


*[√] Проблема со списком интерфейсов. Вынимает лишь часть данных, а попытка вынять 
	последние данные приводит к ошибке. Наверное гибнут документы, а новые документы
	создаются поверх. Вот так вот. Надо бы сделать копирование списков документов?
	Да и что это за странное хранилище, коли при записи в него число ссылок на 
	интерфейс не изменяется и он может быть уничтожен?

*[√] При создании документа быстрыми клавишами и отсылке его не отображает окно
	просмотра документа.

*[√] Очень долго обновляет список документов
	1. Надо пулить создание элементов. И если есть элементы без состояния - то их
		нужно в пул ложить. А также загрузчик списка пользователей - будет 
		иметь кеш пользователей. Может даже загрузчик документов с кешем
		документов.

*[√] Долго ожидает, пока поток проснется - может надо его как демона мочить?

*[√] Давно созданные документы отсылает с датой их создания, а не текущей.
	Во внутренних надо бы писать дату их создания, а в коммуникационных - отсылки.

*[√] Отсылка пересланых документов: не работает удаление.


*[√] При отсылке отвеченного (копии) документа выдает, что у файла нет приложений


***( 21.12.04 )***
### FIXED BUGS ###
*!![√] при ответе на документ перестает открывать приложения к нему. Причем в обоих
	папках: черновиках и коммуникационных

*[√] При ответе на документ и переключении в список черновых документов и выделении
	другого документа - говорит, что такого индекса нет!
	Загружает на 1 документ меньше и при удалении такая же вешь. А в 
	списке отображает все документы, хотя пишет, что их на 1 меньше

*[√] Проблемы при просмотре оператором документов. Он их обратно не развыделяет, как
	прочтенные.

*[√] Проблема при получении сообщения оператором. AccessViolation

### IMPLEMENTED FEATURES ###
+[√] При завершении сеанса надо бы как в аутлуке сообщать- "Подождите, пока завершиться"



***( 20.12.04 )***
### IMPLEMENTED FEATURES ###

+[√] Сделать обработку двойного щелчка: (в коммуникационных - ответить, в черновиках - 
	редактировать)

+[√] Быстрые клавиши на функции.

+[√] Сливать и восстанавливать меню для каждого рабочего места

### FIXED BUGS ###

*[√] При ответе на документ не пересылает его приложения!

*[√] Меню рабочего места не импортируется в основное!



***( 15.12.04 )***

### FIXED BUGS ###

*[√] При переключении между папками считает, что выделены документы, а на самом деле нет.

*[√] При переключении между папками не прокручивает на указанный документ и не обновляет
	из-за этого.

*[√] При ответе на документ и переключении не переключается на него, и 
	не изменяет позицию в дереве папок. 


***( 14.12.04 )***

### FIXED BUGS ###

*[√] При редактировании документа выдает не тому пользователю

*[√] Проблема с посылкой новых документов. Выдает старому. И после этого местами меняет 		кнопки.


***( 13.12.04 )***


### FIXED BUGS ###

*[√] Размеры просмотрщика документов вылазят при разворачивании за экран.

*[√] При загрузке не обновляет первый документ из его содержимого!

*[√] При переключении не возвращает на выбранное место в предыдущий раз.


***( 11.12.04 )***

### FIXED BUGS ###

*[√] Проблема с отображением состояния кнопок редактирования 	черновиков, а также кнопок управления коммуникационными

*[√] Проблема с отображением кому направлен документ при создании
	нового документа.

### IMPLEMENTED FEATURES ###

+[√] Загружать список контактов и обеспечивать создание документов двойным щелчком по
	ним (важно: нужно писать названия департаментов и отделов)


***( 10.12.04 )***

### IMPLEMENTED FEATURES ###

+[√] Сделать навигацию по дереву типов документов. И все перенести в один

+[√] В ини файл вывести строку соединения с базой данных, а в базе данных прописать
	настройки пути к приложениям.

***( 7.12.04 )***

### IMPLEMENTED FEATURES ###
+[√] Создать оповещатель о новых документах


***( 2.12.04 )***

### FIXED BUGS ###

*[√] При старте глючит. Не выдает первый документ из черновиков

*[√] При добавлении не выделяет первый документ

*[√] Создание копии глючит. Выдает не того человека, которого хотел при большом
	количестве документов


*[√] Навигация по внутренним документам глючит - выдает не тех пользователей.
	После создания копии!!


### IMPLEMENTED FEATURES ###

+[√] В базу данных ролей положить еще поле - роль в документообороте: (только 
	те, кто есть в модели) и создать таблицу с расшифровкой этих кодов

***( 26.11.04 )***



### IMPLEMENTED FEATURES ###

+[√] Множественные отсылки. (Сделать выбор нескольких адресатов для письма)

+[√] Прогресс индикатор при загрузке документов

+[√] Перейти на ZEOS. Через 3 слоя ADO - DAO - ODBC все идет так медленно!

+[√] Обеспечить пересылку документов другому пользователю


***( 25.11.04 )***

### IMPLEMENTED FEATURES ###

+[√] В списке документов надо подстраивать размер колонок и секций под текущее
	разрешение (и изменение размера колонки)

***( 24.11.04 )***

### IMPLEMENTED FEATURES ###

+[√] Размер поля кому должен быть до середины редактора документов (
	редактор заголовков будет привязыватся к обоим краям)

+[√] Создать рабочее место администратора

### FIXED BUGS ###

*[√] "Отчет" не выводит в виде документа (заголовке) и все новые типы документов


***( 23.11.04 )***

### FIXED BUGS ###
*[√] Вернуть рабочую версию и сохранить в TeamSource


***( 11.11.04 )***

### FIXED BUGS ###

*[√] В кратком содержании пишет мусор, надо бы сделать 

*[√] Frame_RichEdit не выдает строку с форматированием, а надо бы. Иначе все форматирование
	сводится насмарку

*[√] Редактор  документа не изменяет статус модификации при редактировании


***( 10.11.04 )***

### FIXED BUGS ###

*1[√] Надо доделать редактор документов (тела документа)

### IMPLEMENTED FEATURES ###

+6![√] В редакторе документа перевести документ на RTF (RichEdit) и добавить панель (и 
	обработку) форматирования текста. Также в просмотре отображать RTF


***( 09.11.04 )***

### FIXED BUGS ###
*[√] Отображение статуса соединения с базой данных не соответсвует форме (скину)


***( 08.11.04 )***

### FIXED BUGS ###

*[√] Поток должен выйти сначала, а затем все остальное.

*[√] При выходе ругается на AccessViolation. Возникает в системном логе при очистке
	запроса. Видать сам query очищается, но должен позже всех.
	*Скорее всего глюк выходит в потоке. Может надо синхронизацию поставить?
	*Поставил синхронизацию, а все равно выдает. Буду отлаживать месагами.
	  Так. Очень странно, когда завершаю рабочее место "логофом", выходит без ошибки.
	  По моему удалось локализовать место ошибки.
	 И при сканировании потоков, не выходит, а без сканирования ХР прибивает железно
	Да все тот же глюк: "Системе не удалось найти путь" в потоке по его закрытию
	Просто видать сильно медленный коннект АДО. надо бы поток перевести на более
	быструюю библиотеку ЗЕОС.
	Причина в том, что из потока не удается получить доступ к окружению. Все закрывается
	раньше, чем поток успевает выйти. Поэтому при попытке вывести в лог пишет 
	"Ошибка доступа в память". Сейчас поправил глюк. Но надо же, чтобы поток тоже
	писал в лог, что он завершается или какая ошибка возникла. Надо бы ждать, пока
	поток не закончится, а затем лишь завершать дальше.
	Вот и глюк ценой в неделю :)

*4!![√] При перерегистрации пишет, что файла нет (ThreadError) где?
	Блин. Вот ошибка с этими потоками. :( Надо будет создавать лог с ошибкой потока!
	Локализовал. Теперь пишет в лог и выходит ок.



***( 05.11.04 )***

### FIXED BUGS ###
*[√] При выходе из программы ругается, что не может освободить рабочее место пользователя
	(остановить сканер сообщений)

***( 04.11.04 )***

### FIXED BUGS ###
*[√] При освобождении рабочего места вылетает AccessViolation

*[√] В заголовке программы писать не только тип пользователя, но и его ФИО.

*![√] Почему то не пишет активность удаленного рабочего места в лог, а только меня.


### IMPLEMENTED FEATURES ###

+[√] Надо бы сделать вывод всех ошибок в лог.

+[√] Надо везде, где может возникнуть ошибка прописать текущий путь (как в Unreal)



***( 03.11.04 )***

### FIXED BUGS ###
*[√]  Под 98й глючит. Надо поставить в VPC  и посмотреть, как он там будет себя вести и поставить
	еще удаленный отладчик.

*[√] При выделении пробелом в списке не происходит Click

*[√] Документы не передает

### IMPLEMENTED FEATURES ###
+3![√]  Надо сделать Environment так, чтобы хранил только список объектов глобальных
	(то есть при инициализации ему передают его название и сам объект, а он
	его сохраняет и выдает по имени)



***( 02.11.04 )***

### FIXED BUGS ###
*![√] В черновиках не отображает первый документ при выборе их для отображения.

*[√] Глюк, когда нет документов "Редактировать" нечего, поэтому надо дисаблить.

*[√] Кавычки в тексте сообщения выдают ошибку. Надо изменить.
	(Скорее всего придется кодировать сообщение. Может даже BASE64, хотя можно
	и FlexCompress)

*[√] В редакторе приложений надо блокировать кнопки "удалить" и "редактироватЬ" при 
	пустом списке приложений

*[√] Скролл бар глючит в документах, не прокручивает элемент

*[√] Надо бы мои отосланные документы писать в начале, а не в конце


### IMPLEMENTED FEATURES ###

+9![√] Не выделяет новые документы (должен ставить им флажок непрочитанного или выделять)

+[√] После ввода пароля нажатие ввода переходит в регистрацию

+[√] Надо бы брать наименование роли пользователя из базы данных

+[√] Написать загрузчик (копировщик) рабочего места (из каталога установки)



***( 01.11.04 )***

### FIXED BUGS ###
*[√] Размеры столбцов "От кого", "Кому" - 170, "Краткое содержание" - 170

*[√] По окончанию редактирования не обновляет документ

*[√] По окончанию редактирования не пишет кому пришел документ

*[√] В редакторе документа надо бы отображать текущий вид документа по выданному.

*[√] Глюк несоответствия. В базе данных все начинается с 1, в программе с 0.
	Надо сделать, чтобы все редакторы (документа) выдавали внутренний
	индекс элемента, а при сохранении и загрузке - модифицировать на 1.

*[√] Надо переключаться на доступные типы документов в редакторе документа, при
	изменении текущего направления.

*[√] Пишет несколько типов документов, хотя должен отображать один тип документа
	для каждого направления (а он грузит два раза "Служебная записка")
	"!!" Надо поправить модель: Типы документов будут связаны с направлением,
		как в базе данных!

*![√] Внутри рабочего места оператора приложения отображаются, у офис менеджера
	даже и не думают. Это происходит после отсылки документа.

*![√] Не все вложения отображает в рабоче месте офис менеджера

*![√] При отсылке глючит. Выдает, что отослал документ, а на самом деле он его даже не
	изменил. Надо бы сделать изменения состояния документа в базе данных
    	Последний отосланный документ отображает в черновиках, а не в коммуникационных
 	при последующей перезагрузке.

*[√] Глючит передача документов. Передает, но не все, как то странно получается.
	и теряет некоторые документы.
	!!Причина: Изменяет только первый документ, значит получает неверный индекс
		при создании документа.

*![√] При обновлении сообщений не отображает новый индекс. (пустое поле)
	Также при добавлении нового.

*4![√] При получении времени из базы данных выдает 0. Надо бы получить более точное время.
	В базе хранит верно. (Причем, добавляет верно при создании, а при прочтении
	из базы пишет 0)

*![√] После обновления в список  добавляется вверх, а вид списка сдвигается вниз на 1.	
	(То есть при просмотре 1го видишь последний, 2го - 1й и т.д. )


### IMPLEMENTED FEATURES ###
+[√] Необходимо отображать список человек в редакторе документа и каждому еще
	написать его ФИО (в списке)

+[√] Добавить пересылку между пользователями одной роли (вообще любые пользователи).
	То есть сделать передачу сообщений просто пользователю, а не его роли.

+![√] Надо именовать при редактировании заголовка документа и при просмотре заголовка
	документа (ФИО)

+[√] Добавить кнопку (внутренние документы) "Выделить все"

+4![√] Распакованные документы не имеет лишний раз распаковывать. Достаточно найти, есть
	ли они.



***( 29.10.04 )***

### FIXED BUGS ###
[√] При отсылке документа не отображает коммуникационные документы верно.
	Остаются внутрений список, хотя надо отображать внешний
	?? Причина: Отчего то считает, что текущий список документов - коммуникационный
	!! Устранено: Оказалось при смене закладок не изменял, какая закладка (вид списка
		документов) отображается

[√] При загрузке списка документов в черновики - выдает странные элементы,
	в коммуникационных все хорошо

[√] Отображает документ обратный по индексу относительно позиции, надо загружать
	список задом наперед

[√] Номера документов проставлять надо бы, но для этого надо знать сколько всего документов.
	Поэтому вынесу в другое место.

[√] Глючит компонент заголовка, выравнять его надо по списку. Да и лучше закрепить

[√]  Не обновляет список приложений после редактирования или создания документов,
	а только после перерегистрации.

[√] После редактирования не обновляет отображение документа

[√] При обновлении списка коммуникации не вызывает обновление числа документов.

[√] При пересылке документов не может нормально удалить все выбранные документы
	из списка и глючит.


### IMPLEMENTED FEATURES ###
[√] При сканировании на сообщения он каждое сообщение раз в 5 секунд выдает, а 
	мне важно только, чтобы пришло оповещение, что есть сообщение.
	Надо бы поменять получатель сообщения, чтобы он чистил все сообщения,
	которые пришли на текущий момент и выдавал лишь флаг первого.
	Тогда в рабочем месте я буду загружать заново все документы.
	!!! Назовем его MessagesArrivalChecker

[√] Отображать "..." если больше 1й строки документа в содержании

[√] Не сохраняет приложения, и не выдает ошибки, если не может сохранить приложение.

[√] Время сохраняет не правильно

[√] При редактировании приложения, список приложений должен оставлять активное
	видимым.

[√] Время загружает не правильно, надо бы поправить (пишет что время 0)

[√] Автоматически отображать первый документ, если хоть один есть в списке.


[√] Отослать документ не работает.
	Надо бы сделать серым и обратно, если хотя бы один доступен

[√] Загрузка документов (на новой модели) глючит. Разобраться.


***( 28.10.04 )***

[√] Слишком мощное и монолитное рабочее место пользователя документооборота
	Надо разнести в отдельные классы его функциональность.
	Проследить за тем, чтобы эффективность работы осталась высокой.


***( 27.10.04 )***

[√] Методы IDocument надо бы все отсортировать по алфавиту


***( 26.10.04 )***
[√] Отправки готовых документов нет

[√] Выделения готовых к отправке документов нет

[√] Чек боксы глючат в элементах вида списка. Может просто вернуться к родным?

[√] Отсылает документы не все, а лишь последний

[√] При начале редактирования неверно отображает кому документ послан!

[√] После редактирования отображает кому в виде "Аноним", хотя хранит как "Офис менеджер"

[√] Очищать папку временных документов по выходу.

[√] Новые документы в начало

[√] Обработка вложений работает только в черновиках

[√] Важно! При отсылке документов не очищает список внутренних документов.
	И отправляет лишь последний документ

[√]  Что то там с загрузкой приложений к документу. Надо бы проверить.
	[√] Отчего то сохраняемый файл совсем маленький. Видать не пакует.
	[√]  Сохраняет лишнее расширение в URN
	[√]  Надо сохранять только имя файла в базе данных, а не полный путь к этому 
		файлу, поскольку в архиве только файл.
	[√]  Не распаковывает файлы, чтобы показать приложения

***( 22.10.04 )***

[√]  Что то там с загрузкой приложений к документу. Надо бы проверить.
	[√]  При сохранении пишет неверный номер документа в приложении.
	[√]  Не переписывает файлы на сервер  и ссылки идут на локальные файлы!
	[√]  Пакует файлы, а результат - пустые.
	[√]  При указании имени файла неверно выбирает из пути имя

[√] Если документов нет, то и отобразитель документа не должен быть виден

[√] После создания документа он не отображается в списке документов, а лишь после
	перерегистрации отображается.

[√] При редактировании не выводит изменения, хотя сохраняет.


***( 21.10.04 )***

[√] После изменения структуры перестал реагировать на выбор документов и не отображает
	их

[√] Редактирование документов из черновиков нет

[√] При обновлении списка документов не надо обновлять внутренние, ибо они изменяются
	только пользователем.


***( 15.10.04 )***
[√] Проблема в получении документов. Получает один из пачки, а лишь при перезагрузке
	рабочего места дополучает остальные. 


[√] Нет выбора документов из сообщения (при нажатии на пришедшее сообщение вообще 
	не реагирует) а при новой загрузке поднимает все документы.
	Решение: Просто сделал обновление документов по получению

[√] Сообщения передает как мусор, скорее всего и документы хранятся не в той
	кодовой странице.

[√]  Передача документов и сообщений глючит. Посмотреть что там с ними.
	Не реагирует на новые документы и сообщения, хотя в базу добавляются.


[√]  Оператору пишет, что данные в черновике его, а на самом деле - отосланные
	офис менеджером.
	Причина: При загрузке документов каждый из обеих сторон видит все документы
		которые у них во взаимодействии. Надо будет назначить каждому документу
		еще его принадлежность.


[√] Очень долго завершает работу рабочего места. Надо бы сделать курсор хотя бы.

[√]  Не выбирает документ по умолчанию

[√]  Загружает не те данные для документа. Все нормально, но содержимое не верно.
	(Два раза одно и то же)
	Причина: 1.Скорее всего пишет неверные данные при отправке.

[√] К документу надо бы сделать краткое описание в списке, а не только тип!

[√] При загрузке рабочего места не выбирает первый документ в списке, даже если он 
	есть там.

[√] В заголовке не пишет чье рабочее место


[√]  При загрузке документа не может присвоить типы документов для пользователя
	Пишет "Interface not supported"
	Причина: При присвоении данных пользователя - присваивает типы поддер
		живаемых документов. Но после некоторого времени в списке начинаются
		странные вещи. Тип документа просто исчезает (AccessViolation)
	Решение: Вернулся к старой методике обработки. Причина была в явном освобождении
		интерфейсов, хотя это можно было доверить дельфи.




***( 13.10.04 )***

[√] Кнопки выбора папок не работают в рабочем месте офис менеджера.


***( 08.10.04 )***

[√]  Почему то выходят не все потоки. Главный выходит, а базовый и другие остаются.
	Причина: Элементы на которые существуют ссылки живут значительно дольше
		чем их контейнеры. 
	Решение: Надо найти и устранить потерю памяти.
	История: 1. После очистки всех интерфейсов виснет.
		2. Zeos отчего то не может освободить библиотеку работы с MySQL 4.0

	Так. Глюк в Zeos. Даже пустой проект не смог выйти достойно. Перейду на ADO!!!
			
***( 07.10.04 )***

[√] Терминал стал ужасно тормозным после изменения очистки интерфейсов. 

[√] Терминал кушает неимоверный объем памяти для такого вида приложений.
	Может имеет смысл перейти на .NET?



***( 06.10.04 )***
[√]  В записи сообщения хранить тип сообщения

[√]   Поток сканирования долго не выходит. Видать ошибка возникает или просто
	не может достучаться до закрытой базы. 
	Причина: - Просто больше 1й ссылки на сканер, проследить где еще есть.
		  Блин. При создании компонента ссылка уже на 1 больше - он 
		ведь поддерживает несколько интерфейсов! Надо решить, как
		их можно освободить. Убрал лишний интерфейс. Но все же
		решить надо будет (когда реализую более сложный клиент с 
		несколькими интерфейсами)

	Решение: Убрать интерфейс. (Можно бы просто лишний раз вызвать релиз,
		но это не правильно!)

***( 05.10.04 )***
[√]  Структура колонок и выводимой информации не изоморфна

[√]  Надо бы вставить отображение текущего состояния документа графически и 
	словами

[√]  При загрузке рабочего места офис менеджера отображает документы, что
	идут к нему, а при загрузке рабочего места оператора те же документы
	отображены как черновые. 

[√]  При загрузке документов пишет не роль пользователя от которого тот пришел,
	а его псевдоним. Изменить.

[√]  При загрузке документов неверно пишет от кого пришли документы. Надо
	бы поправить. 
	(TPaperworkUser_Workplace.load_documents_by строка 273)
	Причина: От кого пришли документы определяет по текущему пользова
		телю, а не по реальному.  Из-за этого меняет местами источник
		и назначение документа!
	Решение: 1. Находить пользователя в базе данных и выбирать его имя.
		2. Поменять местами загрузку пользователя.
		

[√]  Странно отображает данные в списке документов. Надо бы структуру колонок
	исправить

[√]  Не может установить число документов в папках офис менеджера (опять глюк
	в том, что число кнопок в секции bs при исполнении 0, а при разработке
	больше!)
	Рабочее место оператора также просто порождено, но все кнопки создаются
	нормально. 

	Причина: 1. При создании просто не создает корректно секцию.
		2. При порождении от предка рабочего места секция копирует
			лишь внешний вид, данные не были скопированы, хотя
			раньше редактор показывал, что все элементы есть!
	Устранено: Создал все недостающие кнопки и все заработало. (муторно
		так работать, может быть просто отказаться от некоторых элементов
		ГИП?)


***( 04.10.04 )***

[√]  Глючит TbsListView - что делать? Перейти на Java, Delphi8.net?
	Решение: 1. Нет. Останусь здесь. Уже месяц работы. Надо бы и к концу
		дело подводить!

[√]  При редактировании документа (пустого) по умолчанию должен стоять 
	первый элемент списка направлений. Но этого нет. Надо поправить.

[√]  При создании документа он делает назначение неотображаемым.
	Надо бы поправить. 
	Причина: 1. После вставления документа в список - он пытается обновить
		интерфейс, но этот список уже активен, так что ничего не 
		происходит.
	Решение: 1. Проверять, какой список активен, и если список коммуникационных
		документов, то только добавлять в интерфейс.

[√]   Рабочее место офис менеджера не загружает документы, что доступны для 
	него.
	Причина:	2. При загрузке документов неверно указан номер канала 
		документов для переданного документа.
		3. При редактировании документа неверно указывается номер
		канала. (надо на 1 больше)



***( 02.10.04 )***
[√]  В протоколах классов указано TAdoQuery, надо будет просто выбрать общий
	для всех предок: TDataSet!!! 
	После рассмотрения иерархии пришел к выводу, что необходимо все 
	же всех переводить на TZQuery. Потому что в TDataSet нет методов
	запроса данных!

***( 01.10.04 )***

[√]  Странный эффект. В визуальном средстве в панели папок все ок. При 
	запуске говорит, что число кнопок 0, хотя все должно работать.
	До этого работало.
	Причина: Внутри в коде изменял название для установки числа документов
	Устранено: Заменены строки

[√]  Отчего то наименования папок документов так и остались старыми
	Идея: Скорее всего где-то хранятся старые ресурсы (может даже и не в
		dfm). Наверное надо бы просто замочить старые ресурсы.
	Причина: При порождении сохраняет старые ресурсы (где вот только?)
	Устранено: Пересоздал заново фрейм от рабочего места пользователя
		документооборота

[√]  В tool_ado_documentMessageSender::TADO_documentMessageReceiver.send
	в строке 58 говорит что SQL задан неверно.
	Причина: Ошибка в SQL
	Устранено: Добавлено "and" в условии

[√] При попытке закрыть приложение поток сканирования приложения не дает
	остановить себя. 
	Причина: Ошибка в потоке не была отловлена, что приводило к его
		подвисанию
	Устранено: Ошибка отлавливается и ничего не делается, кроме вывода
		сообщения (пока)

[√] При инициализации потока сканирования вылетает ошибка 
	Причина:  При создании рабочего места пользователя документооборота
		создается и запускается сканер сообщений. Но инициализация
		его получателем сообщений происходит позже в коде терминала.
	Устранено: Изменил протокол работы с рабочим местом. Теперь надо
		сначала создать, настроить, а лишь потом запускать на сканирование.


[√] Говорит "Не удается выполнить операцию во время асинхронного выполнения"
	при попытке выбрать данные из базы данных.
	Идея: Создать соединение в потоке? Или вызывать synchronize? 
	Причина: Компоненты доступа к АДО не защищены от потоков
	Устранено: Вынес вызов в Synchronize. Установил поток в приоритет
		tpLower;

